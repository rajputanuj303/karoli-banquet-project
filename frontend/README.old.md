# Karoli Banquet Frontend

This folder will contain the React app for the banquet hall website.

## Setup
Run the following commands to create and start the React app:

```
npx create-react-app .
npm start
```
